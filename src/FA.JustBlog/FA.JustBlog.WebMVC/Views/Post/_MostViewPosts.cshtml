@model  IEnumerable<Post>

@*<h3 class="widget_title">Popular Posts</h3>
    @foreach (var item in Model)
    {
        <div class="media post_item shadow-card">
            <a href="@Url.Action("Details", "Post", new {@id = item.Id })">
                <img class="img-right-sidebar" src="~/asset/images/@item.ImageUrl" alt="post">
            </a>
            <div class="media-body">
                <a href="@Url.Action("Details", "Post", new {@id = item.Id })"><h3>@item.Title</h3></a>
            </div>
        </div>
    }
    <div class="br"></div>*@

<div class="list-group">
    <h3>@ViewBag.PartialViewTitle</h3>
    @foreach (var item in Model)
    {
        <a href="@Url.Action("Details", "Post", new {@id = item.Id })" class="list-group-item list-group-item-action">
            <h5>@item.Title</h5>
            <small>3 days ago</small>
            <p class="mb-1">Just as your title and meta description must sell the click, the introduction must sell the read.</p>
        </a>


    }
</div>